/* config.js — все настройки и константы «ДЖЕК» в одном месте.
   Загружается до основного скрипта как обычный <script>, доступен как window.CONFIG. */
window.CONFIG = {
  /* ---------- купон и хранилище ---------- */
  OUT: ["1", "X", "2"],
  STORAGE_KEY: "baltsystem-kupon-v1",
  MAX_CSV: 60000,

  /* ---------- масштаб ---------- */
  ZOOM_MIN: 0.5,
  ZOOM_MAX: 1.5,
  ZOOM_STEP: 0.05,

  /* ---------- бриф-система ---------- */
  BRIEF_CAP_U: 400000,       /* максимум точек во вселенной */
  BRIEF_WARN_WORK: 8e6,      /* предупреждение о долгом счёте */
  BRIEF_MAX_WORK: 1.2e8,     /* потолок — выше не берёмся */
  BRIEF_TIMEOUT_MS: 30000,   /* таймаут жадного построения */

  /* ---------- матожидание ---------- */
  /* Доли призового фонда по категориям (в процентах от оборота; в сумме 90,
     оставшиеся 10 идут в суперприз). */
  EV_SHARE: { g9: 40, g10: 20, g11: 10, e12: 5, e13: 5, e14: 5, e15: 5 },
  EV_JACK14: 0.10,
  EV_JACK15: 0.90,
  EV_MINPOOL: 0.5,           /* минимальная доля пула на исход — защита от деления на ноль */

  /* Поправка к расчётному числу победителей (факт / модель).
     Откалибровано по фактическим выплатам тиражей 4980, 4981, 4984, 4988,
     4990, 4992, 4993, 4997, 5000, 5003, 5005, 5006, 5007. */
  FIX_BASE: { 9: 0.945, 10: 0.927, 11: 0.892, 12: 0.830, 13: 0.70, 14: 1.2, 15: 1.05 },

  /* ---------- отбор строк из системы ---------- */
  PF_VIABLE: 0.6,            /* доля от лучшего шанса 9+, ниже которой строку не берём */
  PF_SCAN: 4000,             /* максимум строк для перебора */

  /* ---------- проценты и кэфы ---------- */
  PCT_GAP: 4,                /* разрыв (п.п.) для стрелки «большинство» */

  /* ---------- расхождения с толпой ---------- */
  /* Пороги проверены на 2856 матчах из 192 тиражей. */
  CROWD_TRAP_GAP: 10,        /* п.п. выше рынка — ловушка */
  CROWD_VALUE_GAP: 5,        /* п.п. ниже рынка — ценность */
  SWAP_MIN: 1,               /* минимальная отдача замены (сброс толпы / потеря вероятности) */
  /* Проверка на 731 тираже (4135–5015): при 15 п.п. выходило ~8,5 замены за тираж,
     приз в 6,8% тиражей; при 5 п.п. — ~2,9 замены, приз в 10,1%. */
  LOSS_CAP: 0.05,            /* максимум потери вероятности за одну замену */

  /* ---------- Flashscore ---------- */
  FS_FEED_HOST: "https://sweet-heart-f51d.d-pavlovski51.workers.dev/?sport=",
  FS_SPORT_ID: { football: 1, hockey: 4 },

  /* ---------- API и зеркало ---------- */
  API_BASE: "https://totobrief.com/api/v1/community/",
  MIRROR_PATH: "data/api/",

  /* ---------- автообновление ---------- */
  LIVE_EVERY_MS: 30 * 1000,   /* счёт идущих матчей — раз в 30 секунд */
  OPEN_EVERY_MS: 7 * 60 * 1000, /* доли игроков во время приёма — раз в 7 минут */
  HIST_CACHE_MS: 6 * 3600 * 1000, /* кэш истории — 6 часов */
  PREV_STALE_MS: 3 * 24 * 3600 * 1000, /* тираж считаем завершённым через 3 дня */

  /* ---------- страница списка тиражей ---------- */
  HIST_PAGES: 6,              /* страниц списка для шкалы ценности (≈ год) */
  KEEP_INFO: 8,               /* сколько drawing-info хранить */

  /* ---------- тема ---------- */
  THEME_KEY: "theme"
};
